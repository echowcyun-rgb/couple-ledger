# 对话框交接指令

## 第一步：总结进度到 HANDOFF.md

请把当前工作进度总结写入项目根目录的 `HANDOFF.md` 文件（覆盖旧内容），供下一个 Cursor 对话继续。格式如下：

```markdown
# Cursor 对话交接 - 情侣记账 PWA

## 项目信息
- 路径：~/Documents/记账app/-pwav1-移植版
- 技术栈：Next.js 16.2.6 + Turbopack + React 19 + TypeScript 5.7 + Tailwind 4 + Supabase + @e965/xlsx + async-mutex
- 启动命令：npm run dev（端口 3006）

## 已完成的优化

### 阶段 0-4 自测+修复 ✅
- xlsx 漏洞修复（@e965/xlsx 替换）
- Supabase 同步竞态+超时（async-mutex + withTimeout + withRetry）
- TypeScript 类型错误修复（31 个）
- ESLint + vitest 接入
- 5 张表 RLS 已全部 DISABLE

### 优化 1：导入智能分类 + 撤销导入 ✅
- 60 条关键词映射（lib/category-keywords.ts）
- 导入预览页（ImportPreviewSheet）
- 撤销导入功能（RevertImportSheet）
- 支付宝 CSV GBK/UTF-8 双解码（lib/csv-decode.ts）

### 优化 2：支付宝 CSV 修复 + 备注截取 ✅
- 动态表头映射
- noteFromDesc 截取前 5 字

### 优化 3：卡片点击 + 描边字体 + 累加模式 ✅
- 卡片 onClick → openUpdateGoal
- goal-card-days 13px 描边
- saveUpdateGoal 改累加模式

### 优化 4：爱心居中 + 玫红色 ✅
- couple-heart flex 居中
- review-plan-save 改 var(--out)

### 优化 5：底部导航栏像素风图标 ✅
- 10 张像素风 PNG（public/tabbar/）
- TABS 加 imgActive 字段
- TabBar 双图切换
- 旧 nav-*.png 已删除

### 优化 6：UI 修复批次 ✅
- 记账按钮绿底删除
- 结余计算核查（加 console.log）
- 存钱卡片字体描边调整
- 流水+复盘数据汇总栏删除
- 数字去逗号（yuan() 去 toLocaleString）

### 优化 7：功能增强 ✅
- 导入后可删除项目（ImportPreviewSheet 加 ✕ 按钮）
- 重复导入提醒（localStorage 存文件指纹）
- 导入导出前选成员（成员选择弹窗）
- 存钱卡片字体调整（hint 放大 + amt 改阴影）
- 底部导航栏水平对齐
- 我的界面改版（名字分列头像下方 + 天数一行排版金黄色）

## 待执行

### 优化 8：存钱激励 + 发薪日提醒 + 成员限制 ⏸️
- 指令文件：`优化8-存钱激励-Cursor指令.md`（项目根目录）
- 5 项改动：
  1. 更新历史可修改（编辑/删除按钮 + 自动重算 current）
  2. 发薪日当天字幕改红色加粗
  3. 发薪日弹窗（sessionStorage 防重复）
  4. 存钱恭喜弹窗（4 档随机文案）
  5. 成员管理最多 2 名

## 待办 backlog
- 导出 xlsx 空分类显示"未分类"
- 启动加载慢 + 旧数据残留
- 单条 deleteTransaction 补云端删除
- Vercel 部署检查（原始任务第二部分）

## 关键文件清单
- lib/constants.ts — TABS 数组配置
- lib/types.ts — 类型定义（GoalHistoryEntry / Goal / Transaction 等）
- lib/format.ts — yuan() 金额格式化
- lib/supabase.ts — 云同步（withTimeout/withRetry/upsert）
- lib/storage.ts — pushToCloud/syncFromCloud（走锁+超时+重试）
- hooks/useLedger.ts — 核心 hook（所有业务逻辑）
- components/TabBar.tsx — 底部导航栏
- components/tabs/HomeTab.tsx — 首页
- components/tabs/FlowTab.tsx — 流水
- components/tabs/ReviewTab.tsx — 复盘
- components/tabs/MineTab.tsx — 我的
- components/modals/UpdateGoalSheet.tsx — 更新存钱进度弹窗
- components/modals/ImportPreviewSheet.tsx — 导入预览
- components/modals/MemberPage.tsx — 成员管理
- components/styles/ledger.css — 全局样式
- app/page.tsx — 主页面（弹窗挂载点）
- .env.local — Supabase 配置

## 注意事项
- 5 张表 RLS 已全部 DISABLE，无需再处理
- room_id=4230 是测试房间，couples 表存在
- 沙箱限制无法启动 dev server，需用户本地跑 npm run dev
- 不要删除 .workbuddy/ 目录（WorkBuddy 工作区记忆）
```

## 第二步：执行优化 8

总结写入 `HANDOFF.md` 后，读取项目根目录的 `优化8-存钱激励-Cursor指令.md` 文件，按照里面的 5 项改动逐一执行：

1. **问题1**：更新历史可修改（UpdateGoalSheet 加编辑/删除按钮 + useLedger 加 editGoalHistory/deleteGoalHistory + GoalHistoryEntry 加 id 字段）
2. **问题2**：发薪日当天字幕改红色加粗
3. **问题3**：发薪日弹窗（app/page.tsx 加 showPaydayAlert state + sessionStorage 防重复）
4. **问题4**：存钱恭喜弹窗（按金额分 4 档随机文案 + celebrate-bounce 动画）
5. **问题5**：成员管理最多 2 名（MemberPage 隐藏新增按钮 + addMember 加防御性检查）

每项改完后简述改动，全部完成后跑 `npm run build` 验证，最后列出改动文件清单和 commit 建议。

如果对话框在中途满了，确保 `HANDOFF.md` 已保存，新对话开始时先读 `HANDOFF.md` 和 `优化8-存钱激励-Cursor指令.md` 继续执行。
