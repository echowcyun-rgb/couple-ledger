# 房号制改造 - 给 Cursor 的执行命令

将情侣账本从单人使用改为多人隔离的「房号制」模式。
按顺序执行以下步骤。

---

## 第 0 步：Supabase 面板（不进 Cursor）

打开 https://supabase.com/dashboard → 你的项目 → SQL Editor

**先执行：清空旧表**
```sql
DROP TABLE IF EXISTS transactions CASCADE;
DROP TABLE IF EXISTS goals CASCADE;
DROP TABLE IF EXISTS members CASCADE;
DROP TABLE IF EXISTS import_batches CASCADE;
DROP TABLE IF EXISTS couples CASCADE;
```

**再执行：建新表**
```sql
CREATE TABLE IF NOT EXISTS couples (
  room_id TEXT PRIMARY KEY,
  created_at BIGINT NOT NULL DEFAULT (EXTRACT(EPOCH FROM NOW()) * 1000)
);

CREATE TABLE IF NOT EXISTS transactions (
  id TEXT PRIMARY KEY,
  room_id TEXT NOT NULL REFERENCES couples(room_id),
  date TEXT NOT NULL,
  type TEXT NOT NULL CHECK (type IN ('out', 'in', 'save')),
  amount REAL NOT NULL,
  category_key TEXT NOT NULL,
  member_id TEXT NOT NULL,
  note TEXT DEFAULT '',
  status TEXT DEFAULT 'confirmed',
  recorder TEXT,
  created_at BIGINT NOT NULL,
  updated_at BIGINT NOT NULL DEFAULT (EXTRACT(EPOCH FROM NOW()) * 1000)
);
CREATE INDEX IF NOT EXISTS idx_transactions_room_date ON transactions(room_id, date DESC);
CREATE INDEX IF NOT EXISTS idx_transactions_room_type ON transactions(room_id, type);

CREATE TABLE IF NOT EXISTS goals (
  id BIGINT PRIMARY KEY,
  room_id TEXT NOT NULL REFERENCES couples(room_id),
  name TEXT NOT NULL,
  emoji TEXT DEFAULT '★',
  current REAL DEFAULT 0,
  target REAL NOT NULL,
  contributions JSONB DEFAULT '{}',
  history JSONB DEFAULT '[]',
  updated_at BIGINT NOT NULL DEFAULT (EXTRACT(EPOCH FROM NOW()) * 1000)
);
CREATE INDEX IF NOT EXISTS idx_goals_room ON goals(room_id);

CREATE TABLE IF NOT EXISTS members (
  id TEXT PRIMARY KEY,
  room_id TEXT NOT NULL REFERENCES couples(room_id),
  name TEXT NOT NULL,
  avatar TEXT DEFAULT '',
  gender TEXT DEFAULT 'other',
  payday INTEGER DEFAULT 10,
  updated_at BIGINT NOT NULL DEFAULT (EXTRACT(EPOCH FROM NOW()) * 1000)
);
CREATE INDEX IF NOT EXISTS idx_members_room ON members(room_id);

CREATE TABLE IF NOT EXISTS import_batches (
  id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
  room_id TEXT NOT NULL REFERENCES couples(room_id),
  ids JSONB NOT NULL,
  source TEXT NOT NULL,
  recorder TEXT NOT NULL,
  count INTEGER NOT NULL,
  time TEXT NOT NULL,
  created_at BIGINT NOT NULL DEFAULT (EXTRACT(EPOCH FROM NOW()) * 1000)
);
CREATE INDEX IF NOT EXISTS idx_import_batches_room ON import_batches(room_id);
```

---

## 第 1 步：改数据模型

复制以下内容粘贴到 Cursor 对话框：

```
项目：~/Documents/记账app/-pwav1-移植版

请帮我改 4 个文件：

1. lib/types.ts — AppState 接口加 roomId: string
2. lib/constants.ts — createDefaultState 函数加 roomId: ''
3. lib/supabase.ts — 所有查询加 .eq('room_id', roomId)，所有写入数据加 room_id 字段。各函数增加 roomId 参数
4. lib/storage.ts — syncFromCloud 函数读 localStorage 的 roomId 并传给 supabase 函数

改完告诉我。
```

---

## 第 2 步：新建房间设置组件

复制以下内容粘贴到 Cursor：

```
在 components/modals/ 下新建 RoomSetup.tsx

功能：首次打开时显示的全屏弹窗，不可关闭（必须创建或加入才能用）

两个状态：
1. 「创建账本」→ 生成 4 位随机数房号 → 插入 Supabase couples 表 → 保存 roomId 到 localStorage('couple-room-id') → 关闭
2. 「加入账本」→ 输入4位房号 → 检查 couples 表是否存在 → 保存 → 关闭

样式：像素风格，和现有组件统一（深色遮罩 + 白底卡片粗边框 + .px-btn 按钮）
导出：export default RoomSetup
```

---

## 第 3 步：修改页面入口

复制到 Cursor：

```
修改 app/page.tsx：
1. 导入 RoomSetup：import RoomSetup from "@/components/modals/RoomSetup"
2. hydration 后检查 localStorage.getItem('couple-room-id')
3. 没有则显示 <RoomSetup onDone={...} />
4. 有则正常显示主界面
```

---

## 第 4 步：我的 Tab 显示房号

复制到 Cursor：

```
修改 components/tabs/MineTab.tsx：
在页面里显示「🏠 房间号：3827」
旁边有个复制按钮（点击复制到剪贴板 + Toast 提示）
从 localStorage.getItem('couple-room-id') 读取
```

---

## 第 5 步：推 GitHub

所有代码改完后，在终端执行：

```bash
cd ~/Documents/记账app/-pwav1-移植版
git add .
git commit -m "feat: add room code system"
git remote add origin https://github.com/echowcyun-rgb/couple-ledger.git
git push -u origin main
```

---

## 第 6 步：部署 Vercel

打开 https://vercel.com → New Project → Import Git Repository → 选 couple-ledger → Deploy

在 Vercel 项目 Settings → Environment Variables 添加：
- NEXT_PUBLIC_SUPABASE_URL = https://shyoirivtyiepncwjygc.supabase.co
- NEXT_PUBLIC_SUPABASE_ANON_KEY = 你的完整 anon key
